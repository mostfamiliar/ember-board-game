<h4>Groups</h4>

{{#each groups as |group|}}
<h5>Game: {{#link-to 'group' group.id}}{{group.title}}{{/link-to}}{{md-btn icon='mdi-action-delete' action='deleteGroup' actionArg=group buttonType='floating' class='btn-delete btn'}}</h5>
{{/each}}
